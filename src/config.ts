// =========================================================================
// くそざこ村 — balance config
//   全てここで一元管理。テストプレイ後の微調整はまずこのファイルを触る。
//   値を変えても型エラーや挙動破綻を起こしにくいよう、構造はフラットに保つ。
// =========================================================================

export const CONFIG = {
  // --- 時間 -----------------------------------------------------------------
  // 1tickあたりの秒数（固定tick）。小さくすると滑らか・CPU負荷UP。
  TICK_DT: 1 / 20,
  // 1シーズンの実時間秒数。短くすると季節イベントを素早く確認できる。
  SECONDS_PER_SEASON: 60,
  // UI再描画の間隔（秒）。HUDの更新頻度。
  UI_REFRESH_SEC: 0.25,
  // オートセーブ間隔（秒）。
  AUTOSAVE_SEC: 5,

  // --- 人口 -----------------------------------------------------------------
  BASE_POP_CAP: 8,
  MAX_CORPSES_VISIBLE: 40,
  // 出産の基礎インターバル（秒）。この秒数ごとに1体生まれる（キャップ余り時）。
  BASE_SPAWN_INTERVAL_SEC: 4,
  // 出産インターバルの揺らぎ（秒）。毎回±rand()*この値。
  SPAWN_INTERVAL_JITTER_SEC: 2,

  // --- ちびわふ個体 ----------------------------------------------------------
  CHIBI_SPEED_MIN: 18,
  CHIBI_SPEED_RANGE: 10,
  CHIBI_MAX_AGE_MIN_SEC: 40,
  CHIBI_MAX_AGE_RANGE_SEC: 80,

  // --- ポイント --------------------------------------------------------------
  // 死因ごとの基本獲得Pは deaths.ts 側で定義。ここは共通倍率のみ。
  GLOBAL_POINT_MULT_BASE: 1.0,

  // --- イベント基礎発生率（毎tick判定、0〜1）---------------------------------
  ONDO_CHANCE_BASE: 0,   // 0だと太鼓やぐらを建てないと発動しない
  FIRE_CHANCE_BASE: 0,
  // イベントの持続秒数
  ONDO_DURATION_SEC: 6,
  FIRE_DURATION_SEC: 5,
  // イベント中の毎tick死亡確率（1体あたり）
  ONDO_KILL_RATE: 0.35,
  FIRE_KILL_RATE: 0.20,

  // --- ハザード（詳細は hazards.ts）-----------------------------------------
  // Safe zone（フラナ周辺は即死ゾーン無効）
  SAFE_ZONE_R: 90,

  // --- デバッグ --------------------------------------------------------------
  // URLクエリ `?debug=1` でON。dev時は自動でON扱い。
  DEFAULT_TIME_SCALES: [1, 4, 16, 64] as const,
} as const;

export type TimeScale = (typeof CONFIG.DEFAULT_TIME_SCALES)[number];
