# 技術仕様

ゲームエンジン側（Vite + PixiJS 8）で問題なく使えるアセット形式を定義する。
`src/render/sprites.ts` の実装と整合させる。

---

## 1. ファイル形式

| 項目 | 仕様 |
|---|---|
| 形式 | PNG（RGBA、8bit/channel） |
| 色空間 | sRGB |
| 圧縮 | 標準 PNG 圧縮（lossless） |
| EXIF/メタデータ | 不要、あっても無視される |

---

## 2. 背景処理

**最優先要件：完全透過 PNG（alpha channel）**

### 2.1 理想形（納品物）

- キャラクターのシルエット以外は **完全透過（alpha = 0）**
- ground shadow / drop shadow は **焼き込まない**（ゲーム内で procedural 描画）
- 背景に薄いグラデーション・テクスチャ禁止

### 2.2 フォールバック（AI で透過 PNG が出せない場合）

やむを得ず白背景 PNG で納品する場合：

| 要件 | 仕様 |
|---|---|
| 背景色 | 完全な純白 `#FFFFFF`（RGB 全 255） |
| 影 | **焼き込み禁止**（影があると flood-fill で抜けず残る） |
| アンチエイリアス | キャラ輪郭のアンチエイリアスは OK |

`src/render/sprites.ts` の `removeBackgroundFloodFill` が 4 隅から flood-fill で
RGB 全 240 以上のピクセルを alpha=0 にする。**影は 240 未満なので残る**。

### 2.3 NG 例

- 白背景 + 薄いグレー地面影 → 影が残る
- 白背景 + 放射グラデーション → 中心付近は flood-fill で抜けない
- 黒背景 / カラー背景 → sprites.ts が対応してない
- **市松模様（透明プレビュー柄）が実ピクセルとして焼き込まれている画像**
  → 透明 PNG ではない。`hasAlpha: no` / RGB なら即 NG

---

## 3. 解像度とフレーミング

### 3.1 キャンバスサイズ

| 用途 | 推奨 |
|---|---|
| 納品時 | **1024 × 1024 px**（正方形） |
| ゲーム内表示 | スケール可変（実表示は 48-80px 程度） |

将来 Retina 対応や高解像度化に備えて 1024 で納品。

### 3.2 キャラクターフレーミング

| 項目 | 仕様 |
|---|---|
| キャラ全身高 | キャンバス高の **70% ± 3%**（約 720px） |
| 水平位置 | キャンバス中央 |
| 足元基準ライン | キャンバス底から **15%** の位置 |
| 上下マージン | 上約 15% / 下約 15% |
| 左右マージン | キャラ輪郭から最低 10% |

### 3.3 ポーズ間の一貫性

- **全ポーズで体幹中心が同じ位置** に来ること
- **全ポーズで足元基準ラインが同じ Y 座標** にあること
  （`splat`、`drown_flail`、空中ポーズ、横寝睡眠など `30_pose_catalog.md` で例外明記したものは除く）
- スケール（体の大きさ）は全ポーズで統一

これが揃わないと、ゲーム内で state 切替時にキャラが跳ねる。

---

## 4. ファイル命名規則

### 4.1 基本ルール

```
NN_poseName.png
```

- `NN`: 2 桁 zero-pad の番号（`01`-`99`）
- `poseName`: 英小文字 + アンダースコア
- 拡張子: `.png` 固定

### 4.2 既存ファイル（変更禁止）

`public/chibiwafu/` と `public/furana/` に配置：

`public/chibiwafu/00_origin.png` は **ちびわふ発注用のデザイン参照専用画像**。
ゲーム内 ChibiState には直接マッピングしない。納品物ではないため、本書 § 2 の
「背景透過 PNG」要件は新規生成アセットに適用し、`00_origin.png` 自体には適用しない。

| # | ファイル名 | ChibiState マッピング |
|---|---|---|
| 01 | `01_normal.png` | idle / chatting |
| 02 | `02_crying.png` | cry |
| 03 | `03_surprised.png` | surprised / staring |
| 04 | `04_angry.png` | angry / eating |
| 05 | `05_sulking.png` | sleep（legacy 割当。見た目は転んだ瞬間・へたり込み・すね倒れ。純睡眠には使わない） |
| 06 | `06_dizzy.png` | dazed |
| 07 | `07_dirty.png` | hurt |
| 08 | `08_sleepy.png` | exhausted（殴られ後・昏睡しかけ・意識もうろう。純睡眠には使わない） |
| 09 | `09_dead.png` | dead |

### 4.3 追加予約（40 ポーズ計画）

| # | ファイル名 | 用途 |
|---|---|---|
| 10 | `10_walk_lean.png` | 歩行 |
| 11 | `11_run_lean.png` | 走行 |
| 12 | `12_reach.png` | 対象へ手を伸ばす・取得前 |
| 13 | `13_hold_stick.png` | 小枝会議・音頭（長い棒ではなく小枝） |
| 14 | `14_cheeky_hips.png` | ドヤ顔 |
| 15 | `15_heavy_cry.png` | 号泣 |
| 16 | `16_cower.png` | 恐怖・しゃがみ |
| 17 | `17_arms_up.png` | 万歳・絶叫 |
| 18 | `18_drown_flail.png` | 溺死前半 |
| 19 | `19_knocked.png` | ボコられ直後 |
| 20 | `20_splat.png` | 衝撃死 |
| 21 | `21_sit.png` | 座り |
| 22 | `22_talk_gesture.png` | 会話身振り |
| 23 | `23_eat_drink.png` | 口元へ運ぶ・摂取中 |
| 24 | `24_work_carry.png` | 開拓・農作業・運搬 |
| 25 | `25_grabbed.png` | 神につままれている |
| 26 | `26_thrown_airborne.png` | 投げられ中 |
| 27 | `27_weather_suffering.png` | 熱波・寒さ・天候苦痛 |
| 28 | `28_sleep_curl.png` | 純睡眠・丸まり |
| 29 | `29_sleep_flat.png` | 野宿・地面寝 |
| 30 | `30_nap_sitting.png` | 座りうとうと |
| 31 | `31_wake_up.png` | 起床・寝ぼけ |
| 32 | `32_plead.png` | お願い・許し乞い |
| 33 | `33_celebrate_jump.png` | 喜び・成功 |
| 34 | `34_sick_fever.png` | 病気・発熱 |
| 35 | `35_smoke_cough.png` | 火事煙・咳き込み |
| 36 | `36_dig_scrape.png` | 掘る・耕す |
| 37 | `37_gather_pickup.png` | 拾う・収穫 |
| 38 | `38_refuse_no.png` | 拒否・いやいや |
| 39 | `39_search_look.png` | 探索・きょろきょろ |
| 40 | `40_lonely_sit.png` | 寂しい・しょんぼり座り |

ポーズ詳細は `30_pose_catalog.md` 参照。

`28`-`30` は純睡眠表現の正規スプライト。現行コードで `sleep` が `05_sulking.png`
へ向いている場合でも、画像仕様上は `05` を純睡眠扱いしない。ゲーム側の状態割当を
拡張する時は `28_sleep_curl.png` を基本 sleep、`29_sleep_flat.png` を野宿/地面寝、
`30_nap_sitting.png` を待機中の居眠りへ割り当てる。

---

## 5. カラーパレット

### 5.1 ちびわふ基本色（実測値、v0.3 で更新）

| 名前 | Hex | 用途 |
|---|---|---|
| 基本クリーム | `#FEFBE7` | 体毛ベース（ちびわふ） |
| 影クリーム | `#F0E5C8` | 体の陰部分（基本色から落とし目） |
| ハイライト白 | `#FFFEF6` | 反射光 |
| 耳内ピンク | `#F5D5D0` | 垂れ耳の内側（ちびわふ・フラナ共通） |
| 頬ピンク | `#F5B5B0` | 頬染め（ちびわふ・フラナ共通） |
| ちびわふ手足肉球ピンク | `#F5B5B0` | ちびわふの手足先端の小さなピンク肉球パッド（頬色と同系） |
| 鼻/瞳影 | `#3A2A1A` | 鼻・瞳の影部分 |
| ちびわふ瞳メイン | `#502C1B` | ちびわふの目の茶色（実測） |

### 5.2 フラナ基本色（ちびわふと色が違う、実測値）

| 名前 | Hex | 用途 |
|---|---|---|
| フラナ体色 | `#FEFAEF` | クリームと白の中間（ちびわふより白め、実測） |
| フラナ影色 | `#F0E8D6` | フラナ体の陰部分 |
| フラナ瞳レッド | `#D33023` | フラナの鮮やかな赤い目（実測） |
| 首輪レッド | `#D12E21` | フラナの赤い首輪（実測、瞳とほぼ同色） |
| 鈴アンティーク金 | `#EFC573` | フラナの首輪のアンティーク金の鈴（実測、純金よりやや渋め） |

### 5.3 装備・装飾色（実測値）

| 名前 | Hex | 用途 |
|---|---|---|
| おむつ白 | `#FAFAFA` | ちびわふ・フラナのおむつの地 |
| ちびわふおむつ肉球 | `#A27250` | ちびわふのおむつ中央の茶色い肉球プリント（実測、想定より明るめ） |
| フラナおむつ肉球 | `#FC97A6` | フラナのおむつ中央の鮮やかなピンク肉球プリント（実測、想定より濃い） |
| 尻尾根元リボン | `#F9C4A1` | ちびわふ・フラナ共通、尻尾根元のピーチピンクのリボン（実測、想定より薄い） |
| アクセサリ赤リボン | `#E05860` | 40_accessory_library.md #16「赤リボン」用。尻尾根元のピーチピンクと区別するためより赤寄り |

> ⚠ 以前の v0.1 では「尻尾先端オレンジ」「スタイ肉球」と誤記していた。
> 正しくは「尻尾根元リボン」「おむつ肉球」。用語に注意。
>
> ⚠ v0.2 までは色値が私の目測推定で実画像とズレていた。v0.3 で実画像を
> Pillow + numpy で実測した正値に置き換え済み。プロンプトに貼る時は本表の
> Hex 値を使用する。

### 5.4 色の使い方

- 彩度は **全体的に低め**（storybook 避けつつも鮮やかすぎない）
- グラデーションは **最小限**（cel shade 寄り）
- 原色（純赤・純青等）は禁止（ただしフラナの赤目・赤首輪は赤系統の中〜濃度で OK）

---

## 6. 納品フォーマット整合

### 6.1 1 発注 = 1 ポーズ = 1 ファイル

- grid 納品禁止（4-pose-in-one は不可）
- 複数ポーズ発注時も **個別ファイル**で納品
- AI が 1 画像内にまとめて出す場合は **クロップして分割保存**

### 6.2 禁止事項

- ファイル内にテキストラベル焼き込み（"walk", "run" 等）
- フレーム・境界線
- 透かし・署名

---

## 7. 変更履歴

- v0.1 初版（既存 sprites.ts 実装に合わせて形式固定）
- v0.2 カラーパレット修正（10_character_design.md v0.3 との整合）：
  - 「スタイ肉球」「スタイ白」→ 「おむつ肉球」「おむつ白」に改名
  - 「尻尾オレンジ（先端）」→ 「尻尾根元リボン」に改名
  - フラナ専用色を追加（体色 / 赤目 / 首輪レッド / 鈴ゴールド / ピンク肉球）
  - アクセサリ赤リボン `#E05860` を § 5.3 に追加（40_accessory_library.md #16 で使用中だったが未登録だった）
- v0.3 全色値を実測値に置換（10_character_design.md v0.4 と同期）：
  - ちびわふ体色 / 茶肉球 / リボン / 瞳の 4 値
  - フラナ体色 / 赤目 / 首輪 / 鈴 / ピンク肉球 の 5 値
  - 全て Pillow + numpy で 01_normal.png から実測、目測推定値を破棄
- v0.4 透過判定の NG 例を追加：
  - 透明プレビューの市松模様が実ピクセルとして焼き込まれた RGB PNG を明示的に NG 化
- v0.5 `public/chibiwafu/00_origin.png` を発注用 reference-only 画像として明記：
  - ChibiState にはマッピングしない
  - 透過 PNG 要件は新規生成アセットに適用し、reference-only 画像には適用しない
- v0.6 30 ポーズ計画へ拡張：
  - 05/08 は既存 runtime 割当を残しつつ、純睡眠ではないと明記
  - 23-30 の予約ファイル名を追加
  - 空中・横寝・溺れなど、足元基準ライン例外を `30_pose_catalog.md` 側で明記する運用にした
- v0.7 `12_reach` と `23_eat_drink` の用途を分離：
  - 12 は対象へ手を伸ばす・取得前
  - 23 は口元へ運ぶ・摂取中
- v0.8 40 ポーズ計画へ拡張：
  - 31-40 の予約ファイル名を追加
  - 背景描画ではなく、単体スプライトの状態差分として使う用途に限定
- v0.9 ちびわふ手足肉球色を追加：
  - 手足先端の小さなピンク肉球パッドを `#F5B5B0` 系として明記
  - おむつ中央の茶色肉球プリントとは別要素として扱う
